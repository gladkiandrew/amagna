/**
 * Frame 2 — VoyageReveal (SCAFFOLD ONLY — do not build yet).
 *
 * Intentionally empty. It exists so /hero-v2 scrolls past the hero and the
 * future scroll-into-view trigger has a real section to attach to. The
 * fire-once ship (right→left) + five agent columns are spec'd in a follow-up.
 */
export function VoyageReveal(): JSX.Element {
  return (
    <section
      aria-label="Voyage reveal — coming soon"
      className="relative min-h-screen w-full bg-brand-deep"
    >
      {/* Frame 2: VoyageReveal — fire-once ship right→left + 5 agent columns, spec'd in follow-up */}
    </section>
  );
}
