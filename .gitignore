# Dependencies
node_modules/
.pnp
.pnp.js

# Production builds
.next/
out/
build/
dist/

# Environment variables — never commit
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.cloudflare
.env.cloudflare.local
.env.*.local
# Editor/CLI backups of env files (e.g. .env.local.save) — also never commit
.env*.save
.env*.bak
.env*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Supabase
.supabase/

# Cloudflare
.wrangler/

# OpenNext build output (Workers target)
.open-next/

# Next-on-pages build output (Pages target, being deprecated)
.vercel/

# Misc
.turbo/
.cache/
coverage/

# Ruflo runtime state (regenerable — config/agents/hooks under .claude/ ARE tracked)
.claude-flow/
.swarm/
.hive-mind/
/memory/

# Scraped lead lists — PII, never commit
leads/

# AI-generated blog drafts (working files; approve in Sapt, do not commit)
apps/marketing/drafts/
drafts/

# Local skill installs — .claude/skills/ symlinks into .agents/; skills-lock.json
# is the committed manifest for reinstalling them on another machine
.agents/
.claude/skills/

# Playwright MCP browser-session artifacts (regenerate on use)
.playwright-mcp/

# Editor-local workspace files
*.code-workspace

# Raw footage and brand-source media — archive outside the repo
# (raw masters live in ~/Movies/Amagna-raw and ~/Pictures/Amagna-brand-source)
*.mov
hf_*.mp4
Pics for Amagna/
Vids for Amagna/
AMG Replica/
